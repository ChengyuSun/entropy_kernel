import copy


def count_label_graphlet_chain(begin_index,chain_lenth,Adj,N):
    a=copy.copy(Adj)
    for i in range(N):
        stack=[]
        stack.append(a)
        if find_chain()
    return


def generate_graphlet_index(label_num):

    return